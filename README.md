Compiler written in <b>C</b> and built with Bison and Flex.
Flex and Bison MiniC compiler, where the MiniC language contains basic C expressions and statements.
The compiler compiles MiniC into assembly code.

